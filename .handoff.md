# 引き継ぎ (.handoff.md)

## 最終更新: 2026-03-14（28回目）

## 作業状態
- **レビュー指摘 Step 2 + Step 3 完了（Codexレビュー全指摘修正完了）**
- `python3 -m pytest tests/ -q` は 255 passed

## 今回の変更（28回目）

### Step 3: 整合性・堅牢性（Codexレビュー指摘修正）

#### storage.py — delete_school_data に api_keys 削除を追加
- 学校全データ削除時に `api_keys` テーブルも削除するよう追加
- `summary` に `api_keys_deleted` カウントを追加

#### storage.py — 監査ログ追記の単一トランザクション統合
- `_get_latest_audit_hash(conn=None)`: コネクション引数を受け取れるよう拡張
- `log_audit_event()`: prev_hash取得とinsertを `engine.begin()` 内で統合
- 並行書き込みによるチェーン断絶リスクを解消

#### storage.py — verify_audit_chain のページング全件走査
- `limit` パラメータを `page_size` に改名
- page_size件ずつループで全件走査（デフォルト1000件/ページ）

#### api/app.py — login / mfa/verify のレート制限
- `_RateLimiter` クラス: インメモリ・スライディングウィンドウ方式
- login: 10回/分、mfa/verify: 5回/分（IPアドレス単位）
- `_get_client_ip()`: X-Forwarded-For 対応
- 超過時は 429 + 日本語メッセージ
- 外部ライブラリ不要（slowapi は不使用、依存なし）

#### テスト
- `tests/test_integrity.py` 新規（9テスト）
- `tests/conftest.py`: レート制限リセットの autouse フィクスチャ追加
- 246 → 255 テスト（+9テスト）

### 変更ファイル（Step 3）
- `storage.py`: delete_school_data api_keys追加、監査ログトランザクション統合、ページング走査
- `api/app.py`: レート制限クラス、login/mfa/verify に適用
- `tests/test_integrity.py`: 新規（Step 3テスト9件）
- `tests/conftest.py`: レート制限リセットフィクスチャ

### Step 2: データ保護の実効化（Codexレビュー指摘修正）

#### storage.py — save_session の平文カラム空値化
- 暗号化有効時、平文カラム（`students`, `ocr_results`）に `"[]"` を書く
- 暗号化無効時は従来通り平文カラムにデータを書く
- データ二重保存（平文+暗号文）のリスクを解消

#### storage.py — MFAシークレットの暗号化保存
- `setup_mfa()`: `encrypt_text()` でシークレットを暗号化して保存
- `_decrypt_user_fields()`: 新規ヘルパー。`get_user()` / `get_user_by_email()` で呼び出し、MFAシークレットとバックアップコードを復号
- 暗号化無効時は従来通り平文保存（後方互換）

#### auth.py — バックアップコードのハッシュ化
- `hash_backup_code()`: SHA-256でハッシュ化
- `hash_backup_codes()`: リスト全体をハッシュ化
- `verify_backup_code()`: ハッシュ照合を優先し、平文照合もフォールバック（旧データ互換）
- `storage.enable_mfa()`: 平文コードを返しつつ、DBにはハッシュ化コードを保存

#### storage.py — 監査ハッシュの署名対象拡張
- `_compute_audit_hash()` に `user_id`, `school_id`, `details`（sort_keys正規化JSON）, `ip_address` を追加
- `log_audit_event()` から新パラメータを渡す
- `verify_audit_chain()` で拡張フィールドを使って検証

#### テスト
- `tests/test_data_protection.py` 新規（18テスト）
- `tests/test_mfa.py` 修正（ハッシュ化対応）
- 228 → 246 テスト（+18テスト）

### 変更ファイル
- `storage.py`: save_session平文空値化、MFA暗号化、監査ハッシュ拡張
- `auth.py`: バックアップコードハッシュ化
- `tests/test_data_protection.py`: 新規（Step 2テスト18件）
- `tests/test_mfa.py`: ハッシュ化対応修正

## 前回の変更（27回目）

### Step 1: 認証の穴塞ぎ（Codexレビュー Critical 2件 + Warning 1件を修正）

#### api/app.py — 全エンドポイントの認証必須化
- 22エンドポイント中20個を `get_optional_user` → `get_current_user`（認証必須）に切り替え
- `/auth/me` のみ `get_optional_user` を残存（認証状態チェック用）
- `_school_id_or_none()` ヘルパーを廃止、全箇所で `current_user.school_id` を直接参照
- `_require_admin()` ヘルパーを新設し、管理者API 6本で統一使用
- 管理者APIの `if current_user and current_user.role != "admin"` パターン（未認証バイパス可能）を完全修正

#### api_client.py — トークン混線修正
- モジュールグローバル `_auth_token` を `contextvars.ContextVar` に移行
- Streamlitの複数教員セッション間でトークンが混線するリスクを解消

#### テスト修正
- `tests/conftest.py`: `admin_user` / `admin_headers` フィクスチャ追加
- `tests/test_api.py`: 全テストに認証ヘッダーを追加。`TestUnauthenticatedAccess`（7テスト）+ 権限テスト（1件）を新規追加
- `tests/test_api_client.py`: api_client経由テストに `set_auth_token()` を事前セット

#### テスト結果
- 220 → 228 テスト（+8テスト）

### 変更ファイル
- `api/app.py`: エンドポイント認証切り替え、ヘルパー関数変更
- `api_client.py`: `contextvars.ContextVar` 導入
- `tests/conftest.py`: admin フィクスチャ追加
- `tests/test_api.py`: 全面書き換え（認証必須対応 + 未認証テスト追加）
- `tests/test_api_client.py`: 認証トークンセット追加

## 前回の変更（26回目）

### システム全体コードレビュー（Codex）
- 全指摘をコードで検証し妥当性確認済み
- Step 1（認証）→ Step 2（データ保護）→ Step 3（整合性・堅牢性）の3段階修正計画を策定

## Codexレビュー修正（全3ステップ完了）
- ✅ Step 1: 認証の穴塞ぎ（27回目）
- ✅ Step 2: データ保護の実効化（28回目）
- ✅ Step 3: 整合性・堅牢性（28回目）

## 前回の変更（25回目）

### 24回目: MFA（多要素認証）の実装
- TOTP多要素認証（Google Authenticator / Authy 等対応、pyotp使用）
- バックアップコード（10個、使い捨て hex 8文字）
- ログインフロー: パスワード → MFA有効時は `mfa_pending` 一時トークン（5分） → TOTP/バックアップコード検証 → 本トークン
- エンドポイント: `/api/v1/auth/mfa/{setup,enable,disable,verify}`

### 25回目: APIキーのKMS移行
- `api_keys` テーブル: 学校×プロバイダー単位で暗号化保存（Fernet、school_id+provider ユニーク制約）
- プロバイダー構築時のフォールバック: リクエストにAPIキーがなければDBから学校のキーを自動取得
- 管理者専用エンドポイント 3本

## フェーズ1B 完了項目まとめ
1. ✅ JWT認証（bcrypt + PyJWT）— access/refresh トークン
2. ✅ ユーザー/学校/権限の DB モデルと CRUD
3. ✅ マルチテナントデータ分離（school_id ベース）
4. ✅ 監査ログ（HMACチェーンによる改ざん検知）
5. ✅ 保存時暗号化（Fernet、暗号化カラムと平文カラムの併存）
6. ✅ 保存期間・削除設計（学校別 retention_days、自動パージ、解約時エクスポート/完全削除）
7. ✅ プライバシーポリシードラフト
8. ✅ MFA（TOTP多要素認証 + バックアップコード）
9. ✅ APIキーのKMS移行（暗号化保存 + DBフォールバック）
10. ✅ **認証必須化**（get_optional_user → get_current_user 全面切り替え）
11. ✅ **トークン混線修正**（contextvars 移行）

## 次回やること
- 麻布「故郷」ルーブリック（azabu_kokyou.yaml）でGemini精緻化テストを実施
- 商品化に向けたモデル選択戦略の検討（コスト vs 品質のトレードオフ）
- プライバシーポリシーの法務レビュー依頼
- 保護者向け説明文書テンプレートの作成
- MFA強制ポリシーの検討
- インシデント対応計画の詳細策定
- Phase 2（運用基盤整備）に進む場合は productization-roadmap.md を確認
